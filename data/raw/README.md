# Raw data (not version-controlled)

Place unmodified source audio, transcripts, and RSS metadata here. A dedicated
acquisition script may create a new episode directory, but must never overwrite
an existing raw file. Transformation and analysis scripts only read this
directory. Keep credentials elsewhere and never commit raw files.
